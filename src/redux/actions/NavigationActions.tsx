import {CommonActions} from '@react-navigation/native';

// Define your navigation actions
export const navigateToConfirmationPage = () => {
  return CommonActions.navigate('Confirmation'); // Replace 'Confirmation' with the name of your desired page or screen
};
